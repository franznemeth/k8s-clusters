[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

# Versions kept in sync with kubespray/requirements.txt (currently v2.31.0)
[packages]
ansible = "11.13.0"
# Needed for community.crypto module
cryptography = "46.0.7"
# Needed for jinja2 json_query templating
jmespath = "1.1.0"
# Needed for ansible.utils.ipaddr
netaddr = "1.3.0"

[dev-packages]

[requires]
python_version = "3.14"
